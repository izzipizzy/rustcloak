/target
node_modules
.DS_Store
engines/
*.sqlite
# App data (profiles, db.sqlite, engines) — synced separately, never in git
/data/

# Tauri / Svelte / Vite (from scaffold)
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
/src-tauri/gen/schemas

# Local design notes — not published
/docs/
